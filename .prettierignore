# Тексты рерайта (F6) — данные, а не исходники. Prettier переносит строки
# внутри абзацев и добавляет отступы, и эти переносы уезжают в поле
# `content` при заливке. Формат файла задан скриптом apply-rewrite.ts:
# front matter, дальше HTML-фрагмент, абзацы одной строкой.
prisma/data/rewrite/
