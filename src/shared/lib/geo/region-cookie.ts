/**
 * Где хранится выбранный регион.
 *
 * Cookie, а не сессия и не профиль: выбор нужен и незалогиненному,
 * и переживает закрытие вкладки, и не требует запроса.
 */
export const REGION_COOKIE = 'et_region';

/** Полгода: сезон плюс подготовка к следующему. */
export const REGION_COOKIE_MAX_AGE = 60 * 60 * 24 * 180;
