import type { Region } from '@/entities/region/model/types';

/**
 * Реестр регионов.
 *
 * Заводится руками, как и посадочные: регион появляется в списке вместе
 * с городами, объектами, текстами и гидами, а не раньше.
 */
export const REGIONS: Region[] = [
  {
    slug: 'krym',
    title: 'Крым',
    pathPrefix: '',
    isPublished: true
  },

  // --- Заготовка: подтверждённого предложения нет -------------------------
  // Запись существует ради того, чтобы механика региона была проверяема на
  // двух регионах, а не на одном: тесты на префиксы и на разрешение региона
  // из адреса ничего не стоят, пока регион ровно один. Наружу заготовка не
  // отдаётся — ни адресом, ни переключателем, ни sitemap.
  {
    slug: 'kavkaz',
    title: 'Кавказ',
    pathPrefix: '/kavkaz',
    isPublished: false
  }
];

/** Регион по умолчанию: с него сайт начинался и им остаётся без выбора. */
export const DEFAULT_REGION_SLUG = 'krym';
