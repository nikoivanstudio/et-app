# SEO

Материалы по поисковому продвижению energy-tur.ru. Составлено 11.09.2026.

| Документ | Что внутри |
| --- | --- |
| [`plan.md`](plan.md) | План внедрения: 8 фаз, 54 задачи с файлами, критериями приёмки, оценками и статусом |

**Ссылки на свёрстанные версии:**

- [План внедрения](https://claude.ai/code/artifact/e16e05f7-1983-4e34-be73-aa9c84d239ef) — то же, что `plan.md`
- [Стратегия](https://claude.ai/code/artifact/70ae2c94-4e65-45f2-9978-e8b890484266) — анализ ниши, конкуренты, семантика, структура сайта, контент-план

## Ход работ

Обновлено 11.09.2026.

| | Статус | Задач |
| --- | --- | --- |
| ✅ | сделано | 6 |
| 🔨 | можно делать сейчас | 29 |
| 🔑 | ждёт доступа (база, Вебмастер, Метрика, сервер, Wordstat) | 10 |
| 👤 | ждёт клиента (данные по турам, прайс, фото, решения) | 9 |

Сделанное лежит в ветке `seo/phase-b-d`, три коммита: описания страниц
и настоящий 404, хлебные крошки с `BreadcrumbList`, схема организации
и вычисляемый год. На прод не выкачено и сборкой не проверено — нужен
прогон `npm test` и `npm run build` на машине с базой.

Полная разбивка — в [`plan.md`](plan.md#статус-работ).

## Коротко о состоянии

**Платформа в порядке, наполнение — нет.** Next.js 16 с серверным рендерингом,
TTFB 172 мс, продуманный билдер метаданных, ISR-sitemap, заголовки безопасности.
Значительная часть SEO-долгов закрыта осознанно.

**Главная проблема — пустой каталог.** Карточка тура, бронь, кабинет партнёра
и модерация написаны, но в базе нет ни одного опубликованного тура и ни одного
гида. В `sitemap.xml` нет адресов `/tour/`, страница `/tours` отдаёт заголовок без
карточек, «Популярные туры» на главной ведут на легаси-статьи. Пока это так,
оптимизировать нечего: трафик пойдёт на страницы, с которых нельзя заказать.

**Цифры обхода 11.09.2026:**

| Показатель | Значение |
| --- | --- |
| URL в `sitemap.xml` | 868 |
| из них в корне одним уровнем | 780 |
| страниц с `description` = литерал `description` | ~92% (46 из 50 в выборке) |
| групп дублей / страниц в них | 35 / 108 |
| страниц пагинации `/posts/N` в sitemap | 76 |
| слагов с мусором из HTML-сущностей | 47 |
| страниц Lorem ipsum и со слагом-UUID | 5 + 2 |
| опубликованных туров и гидов | 0 |

## Что дальше

Со стороны разработки в первую очередь: **D3** (вернуть ISR — сейчас все 868
страниц рендерятся заново на каждый запрос), **B5** (механизм редиректов,
без него не начать склейку дублей), **C5** (`Article` и `TouristAttraction`
на справочнике).

Со стороны клиента — **A2**: список реальных маршрутов. Это главный блокер
проекта, без него фазы E и F работают вхолостую.

Отдельно нужен доступ к базе (**A1**): массовые операции по данным делаются
скриптами, а не руками в админке.
