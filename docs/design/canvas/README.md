# Дизайн-канвас Energy Tour

**Ссылка:** https://claude.ai/code/artifact/c57b4036-c2ba-40c2-975c-6f1448d11df7

Макеты основных экранов. Сняты **с текущего кода** (Figma не тронута), затем к ним
применены правки из ревью — сейчас артборды показывают **целевой дизайн v2**, а не то,
что стоит в проде. Прежнее состояние лежит в [`before/`](before/) и в истории версий по
ссылке выше.

## Артборды

| Файл | Экран | Размер | Источник в коде |
|---|---|---|---|
| `Foundations.dc.html` | Токены v2, типографика, компоненты | 1000×3400 | `globals.css`, `shared/ui`, SCSS-модули |
| `Main.dc.html` | Главная | 390×3760 | `src/views/home/home.tsx` |
| `Catalog.dc.html` | Все туры | 390×2320 | `src/views/tours/ui/layout.tsx` |
| `Tour.dc.html` | Страница тура | 390×3340 | `src/views/tour/ui/tour-main.tsx` |
| `Booking.dc.html` | Заявка на тур | 390×900 | `src/features/booking/ui/booking-form.tsx` |
| `BookingDone.dc.html` | Заявка отправлена | 390×900 | `src/features/booking/containers/booking-button.tsx` |
| `SignIn.dc.html` | Вход | 390×900 | `src/app/(auth)`, `src/features/auth` |
| `Dashboard.dc.html` | Дашборд гида — Заказы | 1440×1010 | `src/templates/guide-dashboard` |
| `Desktop.dc.html` | Десктоп — каталог 1440 | 1440×1940 | новый — в коде такой раскладки ещё нет |
| `Review.dc.html` | Ревью: находки, замеры, варианты | 1200×5300 | замеры по этим же артбордам |

`canvas.json` — раскладка артбордов, стикеры и стартовый вид.

## Как открыть

- В терминале Claude Code: `/artifacts` (`o` — открыть, `c` — скопировать ссылку).
- В браузере: ссылка выше, или галерея `claude.ai/code/artifacts`.
- **Локально, без интернета: открыть `index.html`** — все артборды на одной странице,
  148 КБ, лежит в гите. Собирается из `*.dc.html` + `canvas.json`: `node build-index.mjs`.
  Пересобирать после любой правки артборда.
- `energy-tour-screens.html` — тот же канвас с редактором внутри (3 МБ, не в гите,
  пересоздаётся сидером). Открывается в браузере в режиме просмотра и экспорта PNG/PDF.

## Как правим дальше

1. Правки мышкой прямо в канвасе → **Save** в шапке (или ⌘S) сохраняет новую версию для всех.
2. Правки через Claude: сказать, что поменять. Я редактирую `.dc.html` в этой папке,
   пересобираю канвас и публикую на ту же ссылку. Если вы правили канвас руками,
   я сначала вычитываю живую версию, чтобы ваши правки не потерялись.
3. Артборды проверяю рендером: headless Chrome через системный
   `/Applications/Google Chrome.app` (браузеры Playwright в проекте устаревшие).
   Так ловятся обрезки рамок, переполнения и реальный контраст текста поверх фото.
4. Перенос в код — по таблице «С чего начинать» в `Review.dc.html`: сначала токены в
   `globals.css`, потом `src/shared/ui`, потом страницы.

Файлы `.dc.html` — источник истины для пересборки, их нельзя удалять.

## Токены v2 — что переносить в globals.css

| Токен | Значение | Контраст |
|---|---|---|
| `--cta` / `--on-cta` | `#B8915A` / `#1F1A12` | 5.96:1 |
| `--gold-photo` | `#E8B055` — только поверх скрима | 6.40:1 |
| `--gold-head` | `#8B6F3D` — заголовки на светлом | 4.38:1 |
| `--gold-ink` | `#7D6234` — ссылки и мелкий текст на креме | 5.35:1 |
| `--ink-muted` / `--ink-faint` | `#6B5F47` / `#7F6F52` | 5.86:1 / 4.57:1 |
| `--free-ink` на `--free-bg` | `#4A6626` на `#EDF3E2` | 5.75:1 |
| `--alert-ink` на `--alert-bg` | `#9E3A25` на `#F9E6E2` | 5.65:1 |
| `--scrim-hero` | `180deg, rgba(20,14,6,.84) 0%, .7 32%, .36 58%, .78 100%` | — |
| `--scrim-card` | `180deg, rgba(20,14,6,.35) 0%, 0 38%, .82 100%` | — |
| `--scrim-band` | `180deg, rgba(20,14,6,.62), rgba(20,14,6,.82)` | — |
| радиусы | pill 999 · card 24 · block 16 · control 10 | — |
| интервалы | 48 / 80 / 120, одно наложение −32px | — |

Удаляется: `--light-green: #81d178` (мёртвый), `-webkit-text-stroke` везде,
`bg-green-500` в обоих `LinkButton`.

## Что изменилось против кода

| Экран | Было | Стало |
|---|---|---|
| Кнопка | белый на `green-500`, **2.28:1** | тёмная тушь на золоте, **5.96:1** |
| H1 главной | золото на небе, **1.35:1** | золото на скриме, **5.31:1** |
| H1 каталога | **3.09:1**, худший участок 1.57 | **8.43:1**, худший 6.86 |
| Заголовок карточки | зависел от кадра | **8.4–14.9:1** на скриме |
| Шапка каталога | 844px до первой карточки | 300px, теги-фильтры, две карточки в первом экране |
| Карточка | 5 плавающих чипов 22–38px | скрим + один факт-ряд, тап по всей карточке, сердце 44px |
| Вход | белый shadcn на zinc-800 | логотип, крем, золотая кнопка |
| Дашборд | 11 колонок, 1468px при 1440 | 6 колонок, влезает; «Детали» — действие справа |
| Десктоп | одна колонка 390px | контейнер 1120, сетка 3 в ряд, меню вместо бургера |
| Ритм | `-127px`, `-45px`, `-70px`, `mt-[25vh]` | 48 / 80 / 120 и одно наложение −32px |

## Чего пока нет

hover/focus/disabled, тёмная тема публичного сайта, десктоп страницы тура и главной
(каркас один и тот же — см. `Desktop.dc.html`), экраны регистрации и восстановления
пароля (переиспользуют карточку входа). Обратного экспорта в Figma нет.

Одно решение стоит подтвердить: в дашборде две денежные колонки («Наш доход» и «На
месте») показывали одинаковые суммы — во второй колонке смысла не было, поэтому в
таблице осталась одна, а «На месте к оплате» ушло в сводную карточку сверху. Если это
разные величины, колонку надо вернуть.
